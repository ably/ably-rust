[package]
name = "ably"
version = "0.2.0"
edition = "2021"
description = "Ably client library SDK"
documentation = "https://docs.rs/ably"
readme = "README.md"
homepage = "https://ably.com"
repository = "https://github.com/ably/ably-rust"
license = "Apache-2.0"
keywords = ["ably", "realtime", "pubsub"]

include = [
  "Cargo.toml",
  "LICENSE",
  "src/**/*",
]

[dependencies]
aes = "0.8.1"
async-trait = "0.1"
base64 = "0.13.0"
cipher = "0.4.3"
chrono = { version = "0.4.19", features = ["serde"] }
hmac = "0.12.1"
rand = "0.8.5"
reqwest = { version = "0.11.10", features = ["json"] }
rmp-serde = "1.1.0"
# RTL18-RTL20 delta decoding (bundled, not a user-supplied plugin)
vcdiff-decode = "1"
rmpv = "1.3.1"
serde = { version = "1.0.137", features = ["derive"] }
serde_bytes = "0.11.6"
serde_json = "1.0.81"
serde_repr = "0.1.8"
sha2 = "0.10.2"
tokio = { version = "1.18.2", features = ["time", "sync", "macros", "rt", "net"] }
tokio-tungstenite = { version = "0.21", features = ["native-tls"] }
futures-util = { version = "0.3", default-features = false, features = ["sink"] }
url = "2.2.2"
urlencoding = "2"
cbc = "0.1.2"
num-traits = "0.2.15"
num-derive = "0.4"

tracing = { version = "0.1", optional = true }

[dev-dependencies]
futures = "0.3.21"
tokio = { version = "1.18.2", features = ["full", "test-util"] }
http = "0.2"
# Sandbox-app teardown at process exit: libc::atexit + a purely-blocking
# HTTP client (no async runtime — reqwest's blocking client aborts inside
# an atexit handler because it starts a tokio runtime there)
libc = "0.2"
ureq = "2"
jsonwebtoken = "9"

[features]
# Route library logs to the `tracing` crate when no log_handler is installed.
tracing = ["dep:tracing"]
native-tls-alpn = ["reqwest/native-tls-alpn"]
rustls = ["reqwest/rustls"]
default = ["reqwest/native-tls-alpn"]
